---
- name: "check packages (debian)"
  ansible.builtin.apt:
    pkg: "{{ package.name }}"
    state: "{{ package.state }}"
  with_items: "{{ rsyslog_packages }}"
  loop_control:
    loop_var: package
